namespace JZ.SCENE
{
    /// <summary>
    /// Used to separate out logic for certain types of scenes
    /// </summary>
    public enum SceneType
    {
        menu = 0,
        level = 1,
        cutscene = 2
    }
}
